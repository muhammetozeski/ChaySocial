# Okuma Payi

Kategori: Okuma deneyimi | Efor: S | Rank: 1

## Ne
Bir yeni dosya, uc duzenlenen dosya. Hicbir sey depolanmaz, hicbir sey sunucuya gitmez, imzali PostData.LongBody yalnizca okunur.

1) YENI: MainProject/Text/ReadingLength.cs - `namespace ChaySocial.MainProject.Text`, `public static class ReadingLength`. Sabitler burada durur: `public const int WordsReadEachMinute = 200;`, `public const int ShortestReadMinutes = 1;` (bir dakikadan kisa bir yazi "0 minutes" degil, en az bir dakika diye okunur), `const string SingleMinutePhrase = "about a minute";`, `const string ManyMinutesPhraseFormat = "about {0} minutes";`. Metotlar: `public static int WordsIn(string longBody)` - `WrittenProse.Read(longBody)` cagirir ve her `ProseBlock.Text`'i bosluklardan bolerek (`Split((char[]?)null, StringSplitOptions.RemoveEmptyEntries)`) toplar; fence icindeki `ProseBlockKind.Code` bloklari da sayilir, cunku o satirlar da sayfada duruyor ve atlanirsa okuyucuya yazi oldugundan kisa soylenmis olur. `public static int MinutesFor(int words)` - `Math.Max(ShortestReadMinutes, (int)Math.Round(words / (double)WordsReadEachMinute, MidpointRounding.AwayFromZero))`. `public static string Describe(int minutes)` ve `public static string Describe(string longBody)`. StyleFingerprint icindeki `WordsIn` bilerek kullanilmaz: o private, noktalama ayikliyor ve bir parmak izi olcuyor - dosyanin remarks'inda bu sebep yazilir.

2) MainProject/UI/Elements/Social/PostCard.razor - mevcut `@if (Post.IsLongForm)` dalindaki `@if (!ShowLongFormInFull)` blogu (satir 69-72): `post-card-read-on` dugmesi ile yeni `<span class="post-card-read-length">@readingLengthLabel</span>` bir `<div class="post-card-read-row">` icine alinir. `@code` blogunda `string readingLengthLabel = string.Empty;` alani ve mevcut `OnParametersSet` (satir 387-394) icinde `IsSignatureTrusted`/`CanBeTipped` yaninda `readingLengthLabel = Post.IsLongForm ? ReadingLength.Describe(Post.LongBody) : string.Empty;` - her repaint'te govdeyi yeniden bolmemek icin, ProseView'in `readBody` ile yaptigi gibi. `ShowLongFormInFull` true iken (PostThread) hic cizilmez. Yeni using gerekmez.

3) MainProject/UI/Elements/Social/PostCardCss.razor - mevcut `.post-card-read-on:hover` kuralindan (satir 138-140) hemen sonra: `.post-card-read-row { display: flex; align-items: baseline; gap: @(AppMeasures.Space.Px8)px; }` ve `.post-card-read-length { color: @(AppColors.TextSecondary.ToRgbaHex(true)); font-size: @(AppMeasures.Font.XSmall)px; }`.

4) MainProject/UI/Pages/PostThread.razor.cs - dosyanin basina `using ChaySocial.MainProject.Text;`, sinifa `const string SubtitlePartSeparator = " · ";`, ve `ThreadSubtitle` (satir 288-292) mevcut cevap sayisini uretmeye devam eder; `post is { IsLongForm: true }` iken sonuna `SubtitlePartSeparator + ReadingLength.Describe(post.LongBody)` eklenir - "12 replies · about 7 minutes".

PostCard alti sayfada kullanildigi icin (Wall, Profile, Subject, Search, Group, PostThread) tek bileseni degistirmek hepsine ulasir.

## Neden
Su an "Read on →" kapisinin arkasinda iki paragraf mi yoksa 20.000 karakter mi oldugu belli degil; okuyucu ne kadarlik bir seye giristigini bilmeden tikliyor. Sure yazili oldugunda uzun bir yaziya simdi mi sonra mi baslayacagina karar verebiliyor. Olcu tarayicida, okunan metnin kendisinden cikiyor - ne bir alan eklenir, ne bir sayac tutulur, ne de kimin neye ne kadar baktigi bir yere yazilir.
